import TourStepsContainer from '../components/TourStepsContainer';

import '../style.scss';

const Map = () => (
	<TourStepsContainer>
		<h3 style={{margin: '0 0 0.4rem 0'}}>Map window</h3>
		<p style={{marginBottom: 0}}>Map with country borders.</p>
	</TourStepsContainer>
);

export default Map;
