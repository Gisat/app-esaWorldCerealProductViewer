import TourStepsContainer from '../components/TourStepsContainer';

import '../style.scss';

const Timeline = () => (
	<TourStepsContainer>
		<h3 style={{margin: '0 0 0.4rem 0'}}>Timeline</h3>
		<p style={{marginBottom: 0}}>
			Pick a layer that you want to add to the map.
		</p>
	</TourStepsContainer>
);

export default Timeline;
